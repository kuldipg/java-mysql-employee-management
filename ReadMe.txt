1) Please refer EmpDB.sql, It has MySQL 8.0.25 DB and Table 'Create Queries'.
Also it has 'Insert Queries' for Test Records which needs to be inserted in DB before playing Java Web Project.

2) I have created JSP Servlet MySQL web maven project using IntelliJ Ultimate 2021 IDE.

3) Once DB is created with Test Records and Java Project is run, on login page, userid is scott and password is tiger, this user has 'admin' role.

4) DB has tables set up with foreign key constraints.

5) '\src\main\resources\db.properties' file has confgurations for Database connection